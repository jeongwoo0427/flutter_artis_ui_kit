part of '../../flutter_artis_ui_kit.dart';

class ASampleButton extends StatelessWidget {
  const ASampleButton({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialButton(
      child: Text('Sample Button!'),
      onPressed: (){}); 
  }
}