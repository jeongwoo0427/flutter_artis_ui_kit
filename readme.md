# Artis UI kit for Flutter

Artis 전용 플러터 앱들을 위한 UI 라이브러리입니다.

## How to use

`pubspec.yaml` 파일에 다음과 같이 의존성을 추가합니다.

``` yaml
dependencies:
  flutter_artis_ui_kit:
    path: [라이브러리 경로]/flutter_artis_ui_kit/
  
```
